import { About } from '@/features/about/About';

export const metadata = {
  title: 'About - MUI theme switcher demo',
  description: 'Server-side Theme Switching with MUI',
};

const AboutPage = () => <About />;

export default AboutPage;
